// what the method flat() does is it gets all the arrays and joins them together 


let flintstones = ["Fred", "Wilma"];
flintstones.push(["Barney", "Betty"]);
flintstones.push(["Bambam", "Pebbles"]);


console.log(flintstones.flat());